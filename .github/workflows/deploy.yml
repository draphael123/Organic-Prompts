# Optional: Auto-deploy to Vercel on push
# This workflow is optional - Vercel can auto-deploy without it
# But if you want GitHub Actions to handle it, uncomment below

# name: Deploy to Vercel
# 
# on:
#   push:
#     branches: [ main ]
# 
# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v3
#       - uses: amondnet/vercel-action@v20
#         with:
#           vercel-token: ${{ secrets.VERCEL_TOKEN }}
#           vercel-org-id: ${{ secrets.ORG_ID }}
#           vercel-project-id: ${{ secrets.PROJECT_ID }}
#           vercel-args: '--prod'

